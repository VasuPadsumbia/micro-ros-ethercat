[pytest]
# Cocotb @cocotb.test() coroutines are prefixed with sim_ so pytest
# does not collect them (they need a simulator-provided dut handle).
# All pytest entry points are regular def test_*() functions that invoke
# iverilog + cocotb via the cocotb runner with COCOTB_TESTCASE filtering.
addopts = -v --tb=short
